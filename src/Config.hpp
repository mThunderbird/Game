#pragma once

// Default window / playfield size. Window may still be resizable;
// gameplay layout uses these as the design resolution.
namespace config {

constexpr int kScreenWidth = 1920;
constexpr int kScreenHeight = 1080;

}  // namespace config
